<%= form_for @note, :html => { :class => "form-horizontal note" } do |f| %>

  <% if @note.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @note.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :content, :class => 'form-control' %>
    </div>
    <%=f.error_span(:content) %>
  </div>
  <div class="form-group">
    <%= f.label :notable_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :notable_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:notable_id) %>
  </div>
  <div class="form-group">
    <%= f.label :notable_type, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :notable_type, :class => 'form-control' %>
    </div>
    <%=f.error_span(:notable_type) %>
  </div>
  <div class="form-group">
    <%= f.label :user_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:user_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                notes_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
